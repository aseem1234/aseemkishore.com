# WordPress REST API base URL
# Use the Rocket.net CDN URL until wp.aseemkishore.com SSL is provisioned
WORDPRESS_API_URL=https://cbj27jbfj4.onrocket.site/wp-json/wp/v2
# Optional per-request deadline; defaults to 10000 and is capped at 30000
# WORDPRESS_FETCH_TIMEOUT_MS=10000

# Viral tools (Vercel project env)
TOGETHER_API_KEY=
OPENAI_API_KEY=

# Optional overrides
# WRITER_MODEL=Qwen/Qwen3.7-Max
# WRITER_FALLBACK_MODEL=zai-org/GLM-5.2
# IMAGE_QUALITY=medium

# Vercel AI Gateway continuity (all absent/off by default)
# TWEET_SCORE_GATEWAY_TEXT_QUALIFIED=true
# TWEET_SCORE_GATEWAY_FALLBACK_MODE=off
# TWEET_SCORE_GATEWAY_CANARY_ARMED=true  # temporary during one attended canary only
# CRON_SECRET=                          # Vercel Cron bearer, not a model key
